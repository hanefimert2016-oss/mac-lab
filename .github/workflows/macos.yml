name: MacOS_Hacker_Lab
on: workflow_dispatch

jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Tmate_Terminal_Tuneli_Ac
      uses: mxschmitt/action-tmate@v3
      with:
        limit-access-to-actor: false
